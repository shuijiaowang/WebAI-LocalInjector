# WebAI-LocalInjector
本地go服务提供接口返回代码文本，浏览器插件运行勾选目录然后接收文本直接注入AI网页输入框然后提问

任务拆解，
1.运行一个后端go服务，允许跨域，暴露两个接口。
2.初始化WXT侧边栏实现请求后端并获得数据。
3.初始化WXT注入逻辑。

【任务类型】修改小需求
【核心目标/问题】
初始化go后端，非常简单，
1.无用户，无jwt，无数据库
2.默认监听9006端口，配置文件可以不存在。
3.主要暴露两个接口，用于返回请求。需要传参，具体逻辑暂时不屑，测试阶段返回文本即可
4.一切从简
【明确前提&假设】列清前置判断，不确定项直接提问
【只允许查看】backend
【不要做】不要全项目扫描；不要改无关代码；不要跑全量构建，不要自动执行全量测试；不要循环修复；不要测试我会手动测试的。
【已知信息】报错/现象/复现步骤/已有能力（最多5行，极简表述）
【验收标准】可量化、可验证的成功条件（拒绝“能用就行”）
【输出要求】先给核心结论，再给修改点；代码改动尽量小





【任务类型】优化前端布局样式，打通完整流程。
【核心目标/问题】
目前已完成：前端设置忽略配置，请求后端返回目录并展示可勾选。前端把忽略配置+勾选的文件目录发送给后端，后端返回处理拼接好的文件路径和文本，测试成功。并补充全局提示词/本次提问需求输入框，并展示全部合成文本可快捷复制。
基本流程以及全部打通，需要进行简单的布局美化和简单的调整。

1.补充，新增输出内容时，会向脚本发送通信
async function handleToContent() {
  const [tab] = await browser.tabs.query({ active: true, currentWindow: true })
  await browser.tabs.sendMessage(tab.id, {type: "form_sidepanel"});
  console.log("成功发送消息给页面脚本")
}

2.整合前端布局，目前全是堆砌和测试用的，现在需要面向用户。

感觉只需要一页就行，常用的放上面，不常用的折叠或放下面。

最上面是`本次提问需求`
然后是`输出内容`，//向脚本发送通信直接插入网页
然后是完整提问文本，不需要看，只需要有复制按钮即可，用于手动输入

下面是目录更新查看与勾选。可以添加缩进折叠，有点太长了。
最下面是忽略配置，折叠，和全局提示词（这两个都相对固定）


【明确前提&假设】列清前置判断，不确定项直接提问
【只允许查看】extension\entrypoints\sidepanel，extension\components
【不要做】不要全项目扫描；不要改无关代码；不要跑全量构建，不要自动执行全量测试；不要循环修复；不要测试我会手动测试的。
【已知信息】报错/现象/复现步骤/已有能力（最多5行，极简表述）
【验收标准】可量化、可验证的成功条件（拒绝“能用就行”）
【输出要求】先给核心结论，再给修改点；代码改动尽量小